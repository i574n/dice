open rust_operators


union view_type =
    | Details
    | Table

type account = rust.std_string

type state_data =
    heap {
        accounts : rust.vec (pair bool account)
        transactions_pages : i32
        transactions_view_type : view_type
    }

type global_state = state_core.global_state state_data
